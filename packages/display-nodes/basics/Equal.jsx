import { Equal } from "tabler-icons-react"

export default {
    name: "Equal",
    description: "Tests if things are equal.",
    icon: Equal,
    valueTargets: ["a", "b"],
    valueSources: ["out"],
}