import { EqualNot } from "tabler-icons-react"

export default {
    id: "basic:NotEqual",
    name: "Not Equal",
    description: "Tests if things are not equal.",
    icon: EqualNot,
    
    inputs: ["_a", "_b"],
    outputs: ["$"],
}