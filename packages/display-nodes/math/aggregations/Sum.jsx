import { Sum } from 'tabler-icons-react'

export default {
    id: "math:Sum",
    name: "Sum",
    description: "Adds all items in a list.",
    icon: Sum,
    
    inputs: ["_in"],
    outputs: ["sum"],
}